# MAYA — Database (Canonical)

**Authoritative content:** [`04_DATABASE_DESIGN.md`](04_DATABASE_DESIGN.md)

## Phase 2 note

This data-engineering phase does **not** implement SQLAlchemy models or Flask DB routes. Training corpus metadata lives on disk under `dataset/` and in generated reports (`dataset/reports/`). Application DB tables remain as designed for later product phases.
